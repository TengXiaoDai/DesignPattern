原型模式:在.net的应用,.NET在system命名空间中提供了Icoloneable接口,其中就是唯一的一个方法Clone(),
这样就能够实现接口就可以完成原型模式了。

使用原型模式的场景与优点:
一般在初始化的信息不发生变化的情况下,克隆是最好的办法,着既隐藏了对象的创建细节,又对性能是大大的提高。因为他不用重新初始化对象,而是动态的获取对象运行时的状态。

浅复制与深复制

MemberwiseClone()方法是这样的